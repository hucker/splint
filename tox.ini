[tox]
envlist = py311,py312,py310,py39,lint

[testenv]
changedir = test
setenv =
    PYTHONPATH = {toxinidir}/src:{env:PYTHONPATH}
commands =
    python -m pytest
deps =
    -rrequirements.txt
    .
    pytest